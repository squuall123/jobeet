{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
    <div class="jumbotron">
    <center><h1>Candidacy edit</h1></center>
</div>
</div>

    {{ form_start(edit_form) }}

         <div class="container">
        <div class="row" style="background-color:#D3D3D3; width:500px;position:relative;left:350px;height:300px;border: 2px solid black;">
        <div style="position:relative;left:150px;top:20px;">{{ form_label(edit_form.candidat) }}<br>
        {{ form_widget(edit_form.candidat) }}</div>
        <div style="position:relative;left:150px;top:20px;">{{ form_label(edit_form.contenu) }}<br>
        {{ form_widget(edit_form.contenu) }}</div>
        
<br><br>
       
        <input style="position:relative;left:150px;top:20px;" class="btn btn-warning" type="submit" value="Edit" />
    </div>
</div>
    {{ form_end(edit_form) }}

    <ul>
        
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
