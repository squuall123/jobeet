{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <center><h1>Category edit</h1></center>
        </div>
    </div>
    
<div class="container">
    {{ form_start(edit_form) }}
        {{ form_label(edit_form.nom) }}<br>
        {{ form_widget(edit_form.nom) }}<br><br>
        <input type="submit" value="Edit" class="btn btn-warning" />
    {{ form_end(edit_form) }}

    <nav aria-label="...">
  <ul class="pager">
    <li class="previous"><a href="{{ path('category_index') }}" ><span aria-hidden="true">&larr;</span>Back to the list</a></li>
            </ul>
        </nav>
            
        
        
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
       
    
    </div>
{% endblock %}
