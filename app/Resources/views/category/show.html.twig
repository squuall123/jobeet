{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <center><h1>Category {{ category.id }}</h1></center>
        </div>
    </div>
    
<div class="container">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nom:</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{category.nom}}</td>
            </tr>
        </tbody>
    </table>
</div>
    <div class="container">
        <nav aria-label="...">
  <ul class="pager">
    <li class="previous"><a href="{{ path('category_index') }}" ><span aria-hidden="true">&larr;</span>Back ot the list of categories</a></li>
            </ul>
        </nav>
            
       
        {% if is_granted('ROLE_ADMIN')%}
        
            <a href="{{ path('category_edit', { 'id': category.id }) }}" class="btn btn-warning">Edit</a><br>
        
    {% endif %}
        
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </div>
   {% endblock %}
